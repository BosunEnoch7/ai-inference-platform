services:
  api:
    build:
      context: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      APP_ENV: development
      LLM_PROVIDER: mock
    restart: unless-stopped

